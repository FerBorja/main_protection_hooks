#!/usr/bin/env bash
set -euo pipefail
branch="$(git rev-parse --abbrev-ref HEAD)"
if [[ "$branch" == "main" || "$branch" == "master" ]]; then
  echo "❌ Commits on '$branch' are disabled. Create a feature branch (e.g., feature/xyz)."
  exit 1
fi
